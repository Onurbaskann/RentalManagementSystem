@model KiraTakip.Models.ViewModels.KiraciFormViewModel
@using KiraTakip.Models
@{
    ViewData["Title"] = "Yeni Kiracı";

    string[] errorKeys = new[] {
        "KiraciNo","KiraciKategoriId","SektorId",
        "Ad","VergiNo","VergiDairesi",
        "Telefon","Email","Adres"
    };
    var errorsDict = errorKeys.ToDictionary(
        k => char.ToLowerInvariant(k[0]) + k.Substring(1),
        k => ViewData.ModelState[k]?.Errors.FirstOrDefault()?.ErrorMessage ?? ""
    );
    var errorsJson = System.Text.Json.JsonSerializer.Serialize(errorsDict);
}

<div class="max-w-[800px] mx-auto">
    <div class="page-header">
        <a href="/Kiraci" class="text-[12.5px] text-muted no-underline inline-block mb-2 hover:text-primary transition-colors">← Kiracılar</a>
        <div class="page-title">Yeni Kiracı Ekle</div>
    </div>
    <form asp-action="Ekle" method="post" enctype="multipart/form-data"
          x-data="kiraciFormData()"
          @@submit.prevent="handleSubmit($event)"
          x-cloak>
        @Html.AntiForgeryToken()

        <!-- Temel Bilgiler -->
        <div class="card mb-5">
            <div class="text-[15px] font-bold mb-5 pb-3 border-b border-slate-100 flex items-center gap-2">
                <span class="w-1.5 h-4 bg-indigo-600 rounded-full"></span>
                Temel Bilgiler
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-x-3.5 gap-y-4">
                <div class="form-group mb-0">
                    <label class="form-label">Kiracı No *</label>
                    <input asp-for="KiraciNo" class="form-control font-mono" />
                    <span x-show="errors.kiraciNo" x-text="errors.kiraciNo" class="form-error"></span>
                </div>
                <div class="form-group mb-0">
                    <label class="form-label">Kiracı Kategorisi *</label>
                    <select asp-for="KiraciKategoriId" class="form-control">
                        <option value="">— Seçiniz —</option>
                        @foreach (var item in (IEnumerable<Kategori>)ViewBag.Kategoriler)
                        {
                            <option value="@item.Id" selected="@(Model.KiraciKategoriId == item.Id ? "selected" : null)">@item.Ad</option>
                        }
                    </select>
                    <span x-show="errors.kiraciKategoriId" x-text="errors.kiraciKategoriId" class="form-error"></span>
                </div>
                <div class="form-group mb-0">
                    <label class="form-label">Sektör *</label>
                    <select asp-for="SektorId" class="form-control">
                        <option value="">— Seçiniz —</option>
                        @foreach (var item in (IEnumerable<Kategori>)ViewBag.Sektorler)
                        {
                            <option value="@item.Id" selected="@(Model.SektorId == item.Id ? "selected" : null)">@item.Ad</option>
                        }
                    </select>
                    <span x-show="errors.sektorId" x-text="errors.sektorId" class="form-error"></span>
                </div>
            </div>
        </div>

        <!-- Tüzel Kişi Bilgileri -->
        <div class="card mb-5">
            <div class="text-[15px] font-bold mb-5 pb-3 border-b border-slate-100 flex items-center gap-2">
                <span class="w-1.5 h-4 bg-indigo-600 rounded-full"></span>
                Tüzel Kişi Bilgileri
            </div>

            <div class="form-group mb-3.5">
                <label class="form-label">Firma / Kurum Adı *</label>
                <input asp-for="Ad" class="form-control" placeholder="Örn: Yıldız Yazılım A.Ş." />
                <span x-show="errors.ad" x-text="errors.ad" class="form-error"></span>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-3.5 gap-y-4">
                <div class="form-group">
                    <label class="form-label">Vergi No *</label>
                    <input asp-for="VergiNo" class="form-control" maxlength="10" inputmode="numeric" pattern="\d{10}" placeholder="10 haneli" />
                    <span x-show="errors.vergiNo" x-text="errors.vergiNo" class="form-error"></span>
                </div>
                <div class="form-group">
                    <label class="form-label">Vergi Dairesi *</label>
                    <input asp-for="VergiDairesi" class="form-control" placeholder="Örn: Bornova VD" />
                    <span x-show="errors.vergiDairesi" x-text="errors.vergiDairesi" class="form-error"></span>
                </div>
                <div class="form-group">
                    <label class="form-label">Ticaret Sicil No</label>
                    <input asp-for="TicaretSicilNo" class="form-control" placeholder="Opsiyonel" />
                </div>
                <div class="form-group">
                    <label class="form-label">Mersis No</label>
                    <input asp-for="MersisNo" class="form-control" placeholder="Opsiyonel" />
                </div>
            </div>
        </div>

        <!-- İletişim -->
        <div class="card mb-5">
            <div class="text-[15px] font-bold mb-5 pb-3 border-b border-slate-100 flex items-center gap-2">
                <span class="w-1.5 h-4 bg-indigo-600 rounded-full"></span>
                İletişim Bilgileri
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-3.5 gap-y-4">
                <div class="form-group">
                    <label class="form-label">Telefon *</label>
                    <input asp-for="Telefon" class="form-control" placeholder="0532 111 2233" />
                    <span x-show="errors.telefon" x-text="errors.telefon" class="form-error"></span>
                </div>
                <div class="form-group">
                    <label class="form-label">E-posta *</label>
                    <input asp-for="Email" type="email" class="form-control" placeholder="ornek@email.com" />
                    <span x-show="errors.email" x-text="errors.email" class="form-error"></span>
                </div>
            </div>
            <div class="form-group mb-0">
                <label class="form-label">Adres *</label>
                <textarea asp-for="Adres" class="form-control" rows="2" placeholder="İl / ilçe / mahalle / cadde / no"></textarea>
                <span x-show="errors.adres" x-text="errors.adres" class="form-error"></span>
            </div>
        </div>

        @{
            var belgeTurleri = ViewBag.BelgeTurleri as List<KiraTakip.Models.Entities.BelgeTuru>;
        }
        @if (belgeTurleri != null && belgeTurleri.Any())
        {
        <div class="card mb-5">
            <div class="text-[15px] font-bold mb-5 pb-3 border-b border-slate-100 flex items-center gap-2">
                <span class="w-1.5 h-4 bg-indigo-600 rounded-full"></span>
                Belgeler
            </div>
            <div class="flex flex-col gap-4">
                @foreach (var bt in belgeTurleri)
                {
                    <div class="flex flex-col gap-1">
                        <label class="form-label">
                            @bt.Ad
                            @if (bt.Zorunlu) { <span class="text-red-500 font-bold ml-0.5">*</span> }
                            <span class="text-muted font-normal text-[11.5px] ml-1">(@bt.IzinVerilenUzantilar, maks. @bt.MaxBoyutMb MB)</span>
                        </label>
                        @if (!string.IsNullOrEmpty(bt.Aciklama))
                        {
                            <div class="text-xs text-muted -mt-1 mb-1">@bt.Aciklama</div>
                        }
                        <input type="file" name="dosya_@bt.Id" class="form-control"
                               accept="@string.Join(",", bt.IzinVerilenUzantilar.Split(',').Select(e => "." + e.Trim()))" />
                    </div>
                }
            </div>
        </div>
        }

        <div class="card mt-4 bg-indigo-50/60 border border-indigo-100">
            <div class="text-sm font-semibold text-slate-700 mb-3">İlk Firma Yetkilisi Daveti <span class="text-muted font-normal">(opsiyonel)</span></div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                <div>
                    <label class="form-label">E-posta</label>
                    <input asp-for="IlkYetkiliEmail" type="email" class="form-control" placeholder="yetkili@firma.com" />
                    <span asp-validation-for="IlkYetkiliEmail" class="form-error"></span>
                </div>
                <div>
                    <label class="form-label">Ad Soyad</label>
                    <input asp-for="IlkYetkiliAdSoyad" type="text" class="form-control" placeholder="Ad Soyad" />
                </div>
            </div>
            <div class="text-xs text-muted mt-2">Girilirse, kiracı oluşturulduktan hemen sonra Firma Yetkilisi rolüyle davet e-postası gönderilir.</div>
        </div>

        <div class="flex gap-3 mt-6">
            <button type="submit" class="btn btn-primary px-6">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" class="mr-1.5"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
                Kaydet
            </button>
            <a href="/Kiraci" class="btn btn-secondary px-6">İptal</a>
        </div>
    </form>
</div>

@section Scripts {
<script>
function kiraciFormData() {
    return {
        errors: @Html.Raw(errorsJson),
        validate() {
            var form = this.$el;
            var get = (name) => {
                var el = form.querySelector('[name="' + name + '"]');
                return el ? (el.value || '').trim() : '';
            };
            var e = {};

            if (!get('KiraciNo')) e.kiraciNo = 'Kiracı No zorunludur.';
            if (!get('KiraciKategoriId')) e.kiraciKategoriId = 'Kiracı kategorisi seçilmelidir.';
            if (!get('SektorId')) e.sektorId = 'Sektör seçilmelidir.';
            if (!get('Ad')) e.ad = 'Firma / Kurum Adı zorunludur.';
            var vno = get('VergiNo');
            if (!vno) e.vergiNo = 'Vergi No zorunludur.';
            else if (!/^\d{10}$/.test(vno)) e.vergiNo = 'Vergi No 10 haneli rakamdan oluşmalıdır.';
            if (!get('VergiDairesi')) e.vergiDairesi = 'Vergi Dairesi zorunludur.';
            if (!get('Telefon')) e.telefon = 'Telefon zorunludur.';
            if (!get('Email')) e.email = 'E-posta zorunludur.';
            if (!get('Adres')) e.adres = 'Adres zorunludur.';

            this.errors = e;
            return Object.keys(e).length === 0;
        },
        handleSubmit(ev) {
            if (this.validate()) ev.target.submit();
        }
    };
}
</script>
}
