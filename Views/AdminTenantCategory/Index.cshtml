@model List<KiraTakip.Models.Dtos.CategoryListItemDto>
@{
    ViewData["Title"] = "Kiracı Kategorileri";
    ViewData["UseCentralFeedback"] = true;
}


<div class="page-header flex items-center justify-between">
    <div>
        <div class="page-title">Kiracı Kategorileri</div>
        <div class="page-subtitle">@Model.Count kayıt — çarpan hesaplamalarında kullanılır</div>
    </div>
    <a href="/Admin/TenantCategory/Create" class="btn btn-primary flex items-center gap-2">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        Yeni Kategori
    </a>
</div>

<div class="card p-0 overflow-hidden">
    <div class="table-wrapper">
        <table class="w-full">
            <thead>
                <tr>
                    <th class="w-16 text-center">Sıra</th>
                    <th>Ad</th>
                    <th class="w-24">Durum</th>
                    <th class="w-40"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var k in Model)
                {
                    <tr>
                        <td class="text-center text-muted text-xs">@k.Order</td>
                        <td class="font-medium text-slate-700">@k.Name</td>
                        <td>
                            @if (k.IsActive)
                            { <span class="badge badge-kirali">Aktif</span> }
                            else
                            { <span class="badge badge-gecmis">Pasif</span> }
                        </td>
                        <td class="flex gap-1.5 justify-end">
                            <a href="/Admin/TenantCategory/Edit/@k.Id.ToHashId()" class="btn btn-sm btn-secondary">Düzenle</a>
                            <form method="post" action="/Admin/TenantCategory/ToggleStatus/@k.Id.ToHashId()"
                                  data-confirm="@(k.IsActive ? $"'{k.Name}' pasif yapılsın mı?" : $"'{k.Name}' aktif yapılsın mı?")"
                                  class="inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm @(k.IsActive ? "btn-danger" : "btn-amber")">
                                    @(k.IsActive ? "Pasif Yap" : "Aktif Yap")
                                </button>
                            </form>
                        </td>
                    </tr>
                }
                @if (!Model.Any())
                {
                    <tr><td colspan="4" class="text-center py-8 text-muted">Henüz kiracı kategorisi tanımlanmamış.</td></tr>
                }
            </tbody>
        </table>
    </div>
</div>
