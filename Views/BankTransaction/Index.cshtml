@model KiraTakip.Models.Common.PagedResult<KiraTakip.Models.Dtos.BankaHareketiListItemDto>
@using KiraTakip.Models
@using KiraTakip.Models.Common
@using KiraTakip.Models.Dtos
@{
    ViewData["Title"] = "Banka Hareketleri";
    string durum = ViewBag.Durum ?? "tum";
    var q = (TableQuery)ViewBag.Query;
    bool canImport = User.HasPermission(PermissionCatalog.Payment.ImportBankStatement);
    bool canMatch  = User.HasPermission(PermissionCatalog.Payment.MatchBankTransaction);
    var pagination = new PaginationModel { Page = Model.Page, Size = Model.Size, Total = Model.Total, BasePath = "/BankTransaction", Extra = q.ToQueryDict() };
}

<div class="page-header flex items-center justify-between">
    <div>
        <div class="page-title">Banka Hareketleri</div>
        <div class="page-subtitle">@Model.Total kayıt</div>
    </div>
    @if (canImport)
    {
        <a href="/BankTransaction/Import" class="btn btn-primary flex items-center gap-2">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M17 8l-5-5-5 5M12 3v12"/></svg>
            CSV İçe Aktar
        </a>
    }
</div>

@if (TempData["Success"] != null)
{
    <div class="bg-emerald-50 border border-emerald-200 text-emerald-800 p-3 rounded-lg mb-4 text-[13.5px] font-medium">
        ✓ @TempData["Success"]
    </div>
}

<!-- Filtreler -->
<div class="flex gap-1.5 flex-wrap mb-5">
    @{
        var filters = new[] { ("tum","Tümü"), ("eslestirilmedi","Eşleştirilmedi"), ("eslesti","Eşleşti"), ("manuel","Manuel Eşleşti") };
    }
    @foreach (var (key, label) in filters)
    {
        bool isActive = durum == key;
        var url = key == "tum" ? "/BankTransaction" : $"/BankTransaction?durum={key}";
        <a href="@url" class="btn btn-sm @(isActive ? "btn-primary" : "btn-secondary text-slate-600 bg-white border-slate-200")">@label</a>
    }
</div>

<div class="card p-0 overflow-hidden">
    <form method="get" class="flex flex-wrap items-center gap-3 p-3 bg-slate-50/50 border-b border-slate-200" action="/BankTransaction">
        <input type="search" name="q" value="@q.Q" class="form-control w-full max-w-xs text-xs font-medium" placeholder="Ara: açıklama, karşı taraf…" />
        <div class="flex items-center gap-2">
            @await Html.PartialAsync("_DateTimePicker", null, new ViewDataDictionary(ViewData) {
                { "Name", "from" }, { "Value", q.From?.ToString("yyyy-MM-dd") ?? "" },
                { "Mode", "date" }, { "Field", "from" }, { "Compact", true }, { "WrapperClass", "w-36" }
            })
            <span class="text-muted">–</span>
            @await Html.PartialAsync("_DateTimePicker", null, new ViewDataDictionary(ViewData) {
                { "Name", "to" }, { "Value", q.To?.ToString("yyyy-MM-dd") ?? "" },
                { "Mode", "date" }, { "Field", "to" }, { "Compact", true }, { "WrapperClass", "w-36" }
            })
        </div>
        <div class="flex items-center gap-2">
            <input type="number" name="min" value="@q.Min" step="0.01" class="form-control w-24 text-xs font-medium" placeholder="Min ₺" />
            <input type="number" name="max" value="@q.Max" step="0.01" class="form-control w-24 text-xs font-medium" placeholder="Max ₺" />
        </div>
        @if (durum != "tum") { <input type="hidden" name="durum" value="@durum" /> }
        <button type="submit" class="btn btn-primary btn-sm px-4">Filtrele</button>
        @if (!string.IsNullOrEmpty(q.Q) || q.From.HasValue || q.To.HasValue || q.Min.HasValue || q.Max.HasValue)
        {
            <a href="/BankTransaction@(durum != "tum" ? $"?durum={durum}" : "")" class="btn btn-secondary btn-sm text-slate-500">Temizle</a>
        }
    </form>
    <div class="table-wrapper">
        <table class="w-full">
            <thead>
                <tr>
                    <th class="w-28">Tarih</th>
                    <th>Açıklama</th>
                    <th class="w-48">Karşı Taraf</th>
                    <th class="w-32 num">Amount</th>
                    <th class="w-24 cell-center">Banka</th>
                    <th class="w-32">Durum</th>
                    @if (canMatch) { <th class="w-24"></th> }
                </tr>
            </thead>
            <tbody>
                @foreach (var b in Model.Items)
                {
                    string eslesmeCls = b.MatchStatus switch {
                        BankMatchStatus.Matched        => "badge-kirali",
                        BankMatchStatus.ManuallyMatched  => "badge-kirali",
                        _                                 => "badge-bos"
                    };
                    string eslesmeLabel = b.MatchStatus switch {
                        BankMatchStatus.Matched        => "Eşleşti",
                        BankMatchStatus.ManuallyMatched  => "Manuel",
                        _                                 => "Bekliyor"
                    };
                    <tr>
                        <td>@b.TransactionDate.ToString("dd.MM.yyyy")</td>
                        <td class="max-w-[300px] truncate" title="@b.Aciklama">@b.Aciklama</td>
                        <td class="text-muted truncate max-w-[150px]">@(b.SenderInfo ?? b.SenderIban ?? "—")</td>
                        <td class="num">@b.TransactionAmount.ToString("N2") ₺</td>
                        <td class="cell-center">@b.BankCode</td>
                        <td><span class="badge @eslesmeCls">@eslesmeLabel</span></td>
                        @if (canMatch)
                        {
                            <td class="text-right">
                                @if (b.MatchStatus == BankMatchStatus.Unmatched)
                                {
                                    <a href="/BankTransaction/EslestirSec/@b.Id.ToHashId()" class="btn btn-sm btn-primary">Eşleştir</a>
                                }
                            </td>
                        }
                    </tr>
                }
                @if (Model.Total == 0)
                {
                    <tr><td colspan="7" class="text-center py-12 text-muted italic">Filtreye uygun hareket bulunamadı.</td></tr>
                }
            </tbody>
        </table>
    </div>
    @await Html.PartialAsync("_Pagination", pagination)
</div>

