@using Microsoft.AspNetCore.Identity
@using KiraTakip.Models
@using KiraTakip.Services.Interfaces
@using KiraTakip.Extensions
@inject UserManager<ApplicationUser> UserManager
@inject SignInManager<ApplicationUser> SignInManager
@inject IUserRolService UserRolService
@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    ApplicationUser? currentUser = null;
    string userInitials = "KT";
    string userAdSoyad = "Demo Kullanıcı";
    string userRole = "Kullanıcı";

    if (SignInManager.IsSignedIn(User))
    {
        currentUser = await UserManager.GetUserAsync(User);
        if (currentUser != null)
        {
            userAdSoyad = currentUser.AdSoyad ?? currentUser.Email ?? "Kullanıcı";
            userInitials = string.Join("", userAdSoyad.Split(' ')
                .Where(p => p.Length > 0).Take(2).Select(p => char.ToUpper(p[0])));

            var roles = await UserRolService.GetUserRolesAsync(currentUser.Id);
            userRole = roles.FirstOrDefault() ?? "Kullanıcı";
        }
    }

    var hasAnyParamAccess = User.HasModuleAccess("Internal.BirimTuru") ||
                            User.HasModuleAccess("Internal.KiraciKategori") ||
                            User.HasModuleAccess("Internal.Sektor") ||
                            User.HasModuleAccess("Internal.BorcTipi") ||
                            User.HasModuleAccess("Internal.TasinmazTipi") ||
                            User.HasModuleAccess("Internal.Tarife") ||
                            User.HasModuleAccess("Internal.BelgeTuru");
}

<aside class="sidebar" :class="{ 'is-open': $store.ui.sidebarOpen }">
    <div class="sidebar-header flex items-center gap-3">
        <div class="sidebar-logo flex-shrink-0">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect width="28" height="28" rx="8" fill="#1a6b5c"/>
                <path d="M7 20V11L14 7L21 11V20H17V15H11V20H7Z" fill="white"/>
            </svg>
        </div>
        <div class="overflow-hidden">
            <div class="sidebar-title leading-tight">Kira Takip</div>
            <div class="sidebar-subtitle leading-tight">Yönetim Paneli</div>
        </div>
    </div>

    <nav class="sidebar-nav">
        <a href="/" class="nav-item @(currentController == "Home" ? "nav-item-active" : "")">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
            Anasayfa
        </a>
        @if (User.HasModuleAccess("Internal.Tasinmaz"))
        {
            <a href="/Tasinmaz" class="nav-item @(currentController == "Tasinmaz" ? "nav-item-active" : "")">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
                Taşınmazlar
            </a>
        }
        @if (User.HasModuleAccess("Internal.Kiraci"))
        {
            <a href="/Kiraci" class="nav-item @(currentController == "Kiraci" ? "nav-item-active" : "")">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
                Kiracılar
            </a>
        }
        @if (User.HasModuleAccess("Internal.Sozlesme"))
        {
            <a href="/Sozlesme" class="nav-item @(currentController == "Sozlesme" ? "nav-item-active" : "")">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
                Sözleşmeler
            </a>
        }
        @if (User.HasModuleAccess("Internal.Rezervasyon"))
        {
            <a href="/Rezervasyon" class="nav-item @(currentController == "Rezervasyon" ? "nav-item-active" : "")">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
                Rezervasyonlar
            </a>
        }
        @if (User.HasModuleAccess("Internal.Odeme"))
        {
            <a href="/Tahakkuk" class="nav-item @(currentController is "Tahakkuk" or "Odeme" ? "nav-item-active" : "")">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="5" width="20" height="14" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg>
                Tahakkuklar
            </a>
            <a href="/BankaHareketi" class="nav-item nav-item-sub @(currentController == "BankaHareketi" ? "nav-item-active" : "")">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/></svg>
                Banka Hareketleri
            </a>
            <a href="/Rapor" class="nav-item nav-item-sub @(currentController == "Rapor" ? "nav-item-active" : "")">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
                Tahsilat Raporu
            </a>
        }
        @if (User.HasModuleAccess("Internal.ManuelBorc"))
        {
            <a href="/ManuelBorc" class="nav-item @(currentController == "ManuelBorc" ? "nav-item-active" : "")">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="12" y1="18" x2="12" y2="12"/><line x1="9" y1="15" x2="15" y2="15"/></svg>
                Manuel Borçlar
            </a>
        }

        @if (User.HasModuleAccess("System"))
        {
            <div class="nav-separator"></div>
            @if (User.HasModuleAccess("System.Kullanici"))
            {
                <a href="/Admin/Kullanicilar" class="nav-item @(currentController == "AdminUser" ? "nav-item-active" : "")">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><line x1="19" y1="8" x2="19" y2="14"/><line x1="22" y1="11" x2="16" y2="11"/></svg>
                    Kullanıcı Yönetimi
                </a>
            }
            @if (User.HasModuleAccess("System.Rol"))
            {
                <a href="/Admin/Roller" class="nav-item @(currentController == "AdminRol" ? "nav-item-active" : "")">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/><polyline points="15 11 17 13 21 9"/></svg>
                    Rol Yönetimi
                </a>
            }
            @if (User.HasModuleAccess("System.Audit"))
            {
                <a href="/Admin/HareketGecmisi" class="nav-item @(currentController == "AdminHareketGecmisi" ? "nav-item-active" : "")">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
                    Hareket Geçmişi
                </a>
            }
        }

        @if (hasAnyParamAccess)
        {
            <div class="nav-separator"></div>
            <div class="nav-group-label">Parametreler</div>
            @if (User.HasModuleAccess("Internal.BirimTuru"))
            {
                <a href="/Admin/BirimTuru" class="nav-item nav-item-sub @(currentController == "AdminBirimTuru" ? "nav-item-active" : "")">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
                    Birim Türleri
                </a>
            }
            @if (User.HasModuleAccess("Internal.KiraciKategori"))
            {
                <a href="/Admin/KiraciKategori" class="nav-item nav-item-sub @(currentController == "AdminKiraciKategori" ? "nav-item-active" : "")">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
                    Kiracı Kategorileri
                </a>
            }
            @if (User.HasModuleAccess("Internal.Sektor"))
            {
                <a href="/Admin/Sektor" class="nav-item nav-item-sub @(currentController == "AdminSektor" ? "nav-item-active" : "")">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
                    Sektörler
                </a>
            }
            @if (User.HasModuleAccess("Internal.BorcTipi"))
            {
                <a href="/Admin/BorcTipi" class="nav-item nav-item-sub @(currentController == "AdminBorcTipi" ? "nav-item-active" : "")">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2"/><rect x="9" y="3" width="6" height="4" rx="1"/><line x1="9" y1="12" x2="15" y2="12"/><line x1="9" y1="16" x2="13" y2="16"/></svg>
                    Borç Tipleri
                </a>
            }
            @if (User.HasModuleAccess("Internal.TasinmazTipi"))
            {
                <a href="/Admin/TasinmazTipi" class="nav-item nav-item-sub @(currentController == "AdminTasinmazTipi" ? "nav-item-active" : "")">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
                    Taşınmaz Tipleri
                </a>
            }
            @if (User.HasModuleAccess("Internal.Tarife"))
            {
                <a href="/Admin/Tarife" class="nav-item nav-item-sub @(currentController == "AdminTarife" ? "nav-item-active" : "")">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/></svg>
                    Yıllık Tarifeler
                </a>
            }
            @if (User.HasModuleAccess("Internal.BelgeTuru"))
            {
                <a href="/Admin/BelgeTuru" class="nav-item nav-item-sub @(currentController == "AdminBelgeTuru" ? "nav-item-active" : "")">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
                    Belge Türleri
                </a>
            }
        }

        @{
            var canAddTasinmaz = User.HasPermission(PermissionCatalog.Tasinmaz.Create);
            var canAddKiraci = User.HasPermission(PermissionCatalog.Kiraci.Create);
            var canAddSozlesme = User.HasPermission(PermissionCatalog.Sozlesme.Create);
            var canAddRezervasyon = User.HasPermission(PermissionCatalog.Rezervasyon.Create);
            var hasAnyQuickAdd = canAddTasinmaz || canAddKiraci || canAddSozlesme || canAddRezervasyon;
        }

        @if (hasAnyQuickAdd)
        {
            <div class="nav-separator"></div>
            <div class="nav-group-label">Hızlı Ekle</div>
            @if (canAddTasinmaz)
            {
                <a href="/Tasinmaz/Ekle" class="nav-item nav-item-sub">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                    Taşınmaz Ekle
                </a>
            }
            @if (canAddKiraci)
            {
                <a href="/Kiraci/Ekle" class="nav-item nav-item-sub">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                    Kiracı Ekle
                </a>
            }
            @if (canAddSozlesme)
            {
                <a href="/Sozlesme/Ekle" class="nav-item nav-item-sub">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                    Sözleşme Ekle
                </a>
            }
            @if (canAddRezervasyon)
            {
                <a href="/Rezervasyon/Ekle" class="nav-item nav-item-sub">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                    Rezervasyon Ekle
                </a>
            }
        }
    </nav>

    <div class="sidebar-footer">
        <div x-data="{ open: false }" class="relative">
            <button type="button" @@click="open = !open"
                class="sidebar-user flex items-center gap-3 w-full text-left hover:bg-white/10 rounded-xl px-2 py-1.5 transition-all">
                <div class="sidebar-avatar flex-shrink-0">@userInitials</div>
                <div class="flex-1 min-w-0">
                    <div class="sidebar-user-name truncate">@userAdSoyad</div>
                    <div class="sidebar-user-role truncate text-[10px] uppercase font-bold opacity-60 tracking-wider">@userRole</div>
                </div>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
                     class="flex-shrink-0 opacity-50 transition-transform" :class="open ? 'rotate-180' : ''">
                    <polyline points="18 15 12 9 6 15"/>
                </svg>
            </button>

            <div x-show="open" @@click.outside="open = false" x-cloak
                 class="absolute bottom-full left-0 right-0 mb-1 bg-white border border-slate-200 rounded-xl shadow-lg overflow-hidden z-50">
                <a href="/Account/SifreDegistir"
                   class="flex items-center gap-2.5 px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50 transition-colors">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
                        <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                    </svg>
                    Şifre Değiştir
                </a>
                <div class="border-t border-slate-100"></div>
                <form asp-controller="Account" asp-action="Logout" method="post">
                    @Html.AntiForgeryToken()
                    <button type="submit"
                        class="flex items-center gap-2.5 px-4 py-2.5 text-sm text-red-600 hover:bg-red-50 transition-colors w-full text-left">
                        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
                            <polyline points="16 17 21 12 16 7"/>
                            <line x1="21" y1="12" x2="9" y2="12"/>
                        </svg>
                        Çıkış Yap
                    </button>
                </form>
            </div>
        </div>
    </div>
</aside>

